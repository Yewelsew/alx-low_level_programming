#include <stdio.h>
/**
 * main - to prints the name of the file
 * @argc: is an argument counter for char
 * @argv: is an argument value for char
 * Return: null
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - argc]);
	return (0);
}
